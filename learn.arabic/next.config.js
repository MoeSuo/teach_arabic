/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "images.google.com",
    ],
   
  },

  reactStrictMode: true,
}

module.exports = nextConfig
